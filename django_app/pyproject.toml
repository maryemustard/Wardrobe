[project]
name = "wardrobe-django"
version = "0.1.0"
description = "Minimal Django MVP for the Wardrobe app (ENTR 5000)"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=1.0",
    "django>=5.2,<6",
    "pillow>=10",
    # production serving
    "gunicorn>=21",
    "whitenoise>=6",
    "dj-database-url>=2",
    "psycopg[binary]>=3.2",
]
